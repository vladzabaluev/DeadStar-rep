using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Инициализируем для каждой комнаты двери (через дурбаттон) и чуваков
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Если хоть 1 из дверей открыта, то активируем чуваков, которые будут бегать за героем
    //тут реалихуем метод который активирует всё
    //а его вызываем из дукрконтролера
}
